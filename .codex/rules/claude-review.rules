# Repository-relative wrapper bytes are writable during development, so the
# project layer requires explicit approval before reviewer execution.
prefix_rule(
    pattern=["./scripts/claude-review"],
    decision="prompt",
    justification="Repository-relative Claude reviewer execution requires explicit approval.",
)
